<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Welcome <%= userName %> - Movie Recommendation System</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/src/public/style.css">
    <style>
        .movie-img-thumb {
            width: 90px;
            height: 120px;
            object-fit: cover;
            border-radius: 6px;
            margin-right: 1.2rem;
            background: #222;
        }
        .movie-list-item {
            display: flex;
            align-items: flex-start;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <span style="margin-right:auto;color:#ff9800;">Welcome, <%= userName %>!</span>
        <a href="/watchlist">Watchlist</a>
        <a href="/logout">Logout</a>
    </nav>
    <div class="center-container">
        <div class="main-title">All Movies</div>
        <form action="/search" method="get" style="margin-bottom:2rem;display:flex;justify-content:center;">
            <input type="text" name="q" placeholder="Search movies..." value="<%= typeof search !== 'undefined' ? search : '' %>" style="padding:0.6rem 1rem;border-radius:6px;border:none;width:250px;max-width:70vw;">
            <button type="submit" style="margin-left:0.5rem;padding:0.6rem 1.2rem;border-radius:6px;border:none;background:#ff9800;color:#fff;font-weight:bold;cursor:pointer;">Search</button>
        </form>
        <div style="max-height:420px;overflow-y:auto;width:100%;max-width:700px;margin:auto;border-radius:10px;box-shadow:0 2px 16px rgba(0,0,0,0.10);background:rgba(35,35,35,0.1);">
            <ul id="movieList" style="list-style:none;padding:0;margin:0;">
                <% if (movies.length === 0) { %>
                    <li style="color:#ff9800;text-align:center;">No movies found.</li>
                <% } else { %>
                    <% movies.forEach(function(m) { %>
                        <li class="movie-card movie-list-item" data-id="<%= m.m_id %>" style="background:#232323cc;padding:1rem;margin:1rem;border-radius:8px;cursor:pointer;">
                            <img class="movie-img-thumb" src="<%= m.movie_image || '/src/public/default-movie.png' %>" alt="Movie Image" onerror="this.src='/src/public/default-movie.png'">
                            <div>
                                <h3 style="margin:0 0 0.5rem 0;"><%= m.movie_title %></h3>
                                <p style="margin:0 0 0.5rem 0;"><%= m.movie_desc || "" %></p>
                                <small>Category: <%= m.movie_category || "N/A" %> | Language: <%= m.movie_language || "N/A" %></small>
                            </div>
                        </li>
                    <% }) %>
                <% } %>
            </ul>
        </div>
    </div>
    <script>
        document.querySelectorAll('.movie-card').forEach(function(card) {
            card.onclick = function() {
                window.location.href = "/movie/" + card.getAttribute('data-id');
            };
        });
    </script>
</body>
</html>
