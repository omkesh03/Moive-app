<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= movie.movie_title %> - Details</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/src/public/style.css">
    <style>
        .movie-img-detail {
            width: 180px;
            height: 240px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            background: #222;
            display: block;
            margin-left: auto;
            margin-right: auto;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <a href="/">Home</a>
        <a href="/watchlist">Watchlist</a>
        <a href="/logout">Logout</a>
    </nav>
    <div class="center-container">
        <div class="main-title"><%= movie.movie_title %></div>
        <div style="background:#232323cc;padding:2rem;border-radius:12px;max-width:600px;">
            <img class="movie-img-detail" src="<%= movie.movie_image || '/src/public/default-movie.png' %>" alt="Movie Image" onerror="this.src='/src/public/default-movie.png'">
            <p><strong>Description:</strong> <%= movie.movie_desc %></p>
            <p><strong>Category:</strong> <%= movie.movie_category %></p>
            <p><strong>Language:</strong> <%= movie.movie_language %></p>
            <p><strong>Director:</strong> <%= movie.movie_director_name %></p>
            <p><strong>Actors:</strong> <%= movie.movie_actor_1 %>, <%= movie.movie_actor_2 %>, <%= movie.movie_actor_3 %></p>
            <p><strong>Release Date:</strong> <%= movie.movie_release_date ? movie.movie_release_date.toISOString().slice(0,10) : "N/A" %></p>
            <p><strong>Country:</strong> <%= movie.movie_country %></p>
            <button onclick="window.location.href='/movie/<%= movie.m_id %>/rate'">Rate</button>
            <form action="/movie/<%= movie.m_id %>/watchlist" method="POST" style="display:inline;">
                <button type="submit">Add to Watchlist</button>
            </form>
        </div>
    </div>
    <% if (typeof alert !== "undefined" && alert) { %>
        <script>
            alert("<%= alert %>");
        </script>
    <% } %>
    <script>
        // Redirect to login if not logged in
        if (!localStorage.getItem('token')) {
            window.location.href = "/login";
        }
    </script>
</body>
</html>
