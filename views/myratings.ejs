<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= userName %>'s Ratings - Movie Recommendation System</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/src/public/style.css">
    <style>
        .ratings-container {
            max-width: 900px;
            margin: 2rem auto;
            background: #232323cc;
            border-radius: 14px;
            padding: 2.5rem 2rem 2rem 2rem;
            box-shadow: 0 4px 32px rgba(0,0,0,0.18);
        }
        .main-title {
            font-size: 2rem;
            color: #ff9800;
            margin-bottom: 2rem;
            text-align: center;
        }
        .rating-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .rating-item {
            display: flex;
            align-items: flex-start;
            background: #232323;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            padding: 1.2rem 1rem;
            box-shadow: 0 2px 12px rgba(0,0,0,0.10);
        }
        .rating-movie-img {
            width: 80px;
            height: 110px;
            object-fit: contain;
            border-radius: 8px;
            margin-right: 1.5rem;
            background: #181818;
        }
        .rating-details {
            flex: 1;
        }
        .rating-movie-title {
            font-size: 1.2rem;
            color: #ff9800;
            font-weight: bold;
            margin-bottom: 0.3rem;
            text-decoration: none;
        }
        .rating-stars {
            color: #ffd700;
            font-size: 1.1rem;
            margin-bottom: 0.4rem;
            display: inline-block;
        }
        .rating-review {
            color: #fff;
            font-size: 1.05rem;
            margin-bottom: 0.2rem;
        }
        @media (max-width: 700px) {
            .ratings-container { padding: 1rem 0.2rem; }
            .rating-item { flex-direction: column; align-items: stretch; }
            .rating-movie-img { margin: 0 0 1rem 0; }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <span style="margin-right:auto;color:#ff9800;">Welcome, <%= userName %>!</span>
        <a href="/home">Home</a>
        <a href="/watchlist">Watchlist</a>
        <a href="/myratings" style="color:#ff9800;">My Ratings</a>
        <a href="/logout">Logout</a>
    </nav>
    <div class="center-container">
        <div class="ratings-container">
            <div class="main-title">My Ratings</div>
            <% if (alert) { %>
                <div style="color: #ff9800; margin-bottom: 1rem;"><%= alert %></div>
            <% } %>
            <ul class="rating-list">
                <% if (ratings.length === 0) { %>
                    <li style="color:#ff9800;text-align:center;">You have not rated any movies yet.</li>
                <% } else { %>
                    <% ratings.forEach(function(r) { %>
                        <li class="rating-item">
                            <img class="rating-movie-img" src="<%= r.movie_image || '/src/public/default-movie.png' %>" alt="Movie Image" onerror="this.src='/src/public/default-movie.png'">
                            <div class="rating-details">
                                <a href="/movie/<%= r.m_id %>" class="rating-movie-title"><%= r.movie_title %></a>
                                <div class="rating-stars">
                                    <% for (let i = 1; i <= 5; i++) { %>
                                        <% if (i <= r.rating) { %>★<% } else { %>☆<% } %>
                                    <% } %>
                                </div>
                                <div class="rating-review"><%= r.review ? r.review : '' %></div>
                            </div>
                        </li>
                    <% }) %>
                <% } %>
            </ul>
        </div>
    </div>
</body>
</html>
