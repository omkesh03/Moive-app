<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Rate <%= movie.movie_title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/src/public/style.css">
</head>
<body>
    <nav class="navbar">
        <a href="/">Home</a>
        <a href="/watchlist">Watchlist</a>
        <a href="/logout">Logout</a>
    </nav>
    <div class="center-container">
        <div class="main-title">Rate: <%= movie.movie_title %></div>
        <form class="login-form" action="/movie/<%= movie.m_id %>/rate" method="POST">
            <label for="rating">Rating (1-5)</label>
            <select id="rating" name="rating" required>
                <option value="">Select rating</option>
                <% for(let i=1; i<=5; i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                <% } %>
            </select>
            <label for="review">Review</label>
            <textarea id="review" name="review" placeholder="Write your review..." rows="4" style="width:100%;"></textarea>
            <button type="submit">Submit Rating</button>
        </form>
    </div>
    <% if (typeof alert !== "undefined" && alert) { %>
        <script>
            alert("<%= alert %>");
        </script>
    <% } %>
</body>
</html>
